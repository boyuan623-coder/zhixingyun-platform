<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiawa.train.payment.mapper.PayRecordMapper">

    <resultMap id="BaseResultMap" type="com.jiawa.train.payment.domain.PayRecord">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="member_id" jdbcType="BIGINT" property="memberId"/>
        <result column="order_id" jdbcType="BIGINT" property="orderId"/>
        <result column="amount" jdbcType="DECIMAL" property="amount"/>
        <result column="type" jdbcType="CHAR" property="type"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>

    <select id="selectByOrderIdAndType" resultMap="BaseResultMap">
        select id, member_id, order_id, amount, type, create_time
        from pay_record
        where order_id = #{orderId,jdbcType=BIGINT}
          and type = #{type,jdbcType=CHAR}
    </select>

    <insert id="insert" parameterType="com.jiawa.train.payment.domain.PayRecord">
        insert into pay_record (id, member_id, order_id, amount, type, create_time)
        values (#{id,jdbcType=BIGINT}, #{memberId,jdbcType=BIGINT}, #{orderId,jdbcType=BIGINT},
                #{amount,jdbcType=DECIMAL}, #{type,jdbcType=CHAR}, #{createTime,jdbcType=TIMESTAMP})
    </insert>

</mapper>
